---
import { getDict, localePath, type Locale } from '../i18n';
import { PARTICIPANTS, MEMBER_COUNTRIES, PROJECTS, facilityStats } from '../data/initiative';
import StatBar from './StatBar.astro';

interface Props {
  readonly locale: Locale;
}

const { locale } = Astro.props;
const h = getDict(locale).home;
const { facilities } = facilityStats();

const metrics = [
  { kind: 'participants', value: PARTICIPANTS.value, label: h.stats.participants, asOf: h.stats.asOf, href: `${localePath(locale, '/about')}#about-community` },
  { kind: 'facilities', value: facilities, label: h.stats.facilities, href: `${localePath(locale, '/about')}#about-facilities` },
  { kind: 'countries', value: MEMBER_COUNTRIES.latinAmerica, label: h.stats.countries, href: `${localePath(locale, '/about')}#about-community` },
  { kind: 'pilots', value: PROJECTS.length, label: h.stats.pilots, href: localePath(locale, '/projects') },
] as const;
---

<StatBar metrics={metrics} variant="community-cards" />
